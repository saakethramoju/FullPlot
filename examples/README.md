# FullPlot Examples

These examples show the intended engineering workflow:

- `hdf5_plotting/` opens and plots HDF5 files.
- `traces/` creates, filters, and overlays `Trace` objects.
- `maps/` generates rectangular HDF5 maps.

Generated `.h5`, `.png`, and `.pdf` files are ignored by git.
